" Settings:
    let g:python_flavor = get(g:, 'python_flavor', 'python')

    let &shellpipe = '2>&1 >'

" Mappings:
    " Run external commands
    nmap ! :!
    nmap !! :!<Up><Enter>

    " open command line
    noremap <Leader><Leader> :
